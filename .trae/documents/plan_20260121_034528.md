## 现状分析
1. 当前音频文件已经是 `money-back-my-home.wav`，并且时长正好是 **10.00秒**，符合用户要求
2. 音频元素 `heartSutraAudioRef` 已经正确指向了 `/audio/money-back-my-home.wav`
3. 但是，当音频播放失败时，代码会调用 `generateBuddhistSound()` 函数，生成梵音诵经风格的音频，而不是 "ALL MONEY BACK MY HOME" 风格的音频

## 解决方案
将 `generateBuddhistSound()` 函数替换为生成 "ALL MONEY BACK MY HOME" 风格的音频，确保在任何情况下都播放正确的音乐。

## 具体修改
1. 修改 `generateBuddhistSound()` 函数，将其重命名为 `generateMoneyBackMyHomeSound()`
2. 更新函数内部逻辑，生成 "ALL MONEY BACK MY HOME" 风格的音频
3. 在所有调用 `generateBuddhistSound()` 的地方，替换为 `generateMoneyBackMyHomeSound()`
4. 确保生成的音频时长为10秒，与原文件时长匹配

## 预期效果
修改后，无论音频文件播放是否成功，"点亮长明灯"时都会播放10秒钟的 "ALL MONEY BACK MY HOME" 音乐，完全符合用户要求。