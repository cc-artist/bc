1. **更新音频生成脚本**
   - 修改 `generate-audio.js` 脚本，将当前的赛博风格钟声替换为诵经风格音频
   - 使用更复杂的波形组合和调制效果模拟诵经声
   - 保持10秒以内的音频时长
   - 确保音频清晰响亮，符合赛博佛教主题

2. **优化Web Audio API回退方案**
   - 更新 `page.tsx` 中的 `generateBuddhistSound` 函数
   - 将Web Audio API生成的音频从钟声调整为诵经风格
   - 保持相同的音量和播放逻辑
   - 确保在HTML5 Audio播放失败时能无缝切换

3. **重新生成音频文件**
   - 运行更新后的 `generate-audio.js` 脚本
   - 生成新的 `heart-sutra-bell.wav` 文件
   - 验证音频时长和质量

4. **测试功能完整性**
   - 确保长明灯点亮时播放新的诵经音频
   - 验证音频加载成功率
   - 测试不同浏览器环境下的兼容性

通过以上步骤，将长明灯的音频从钟声更换为更符合佛教主题的诵经音频，同时保持良好的用户体验和音频可靠性。