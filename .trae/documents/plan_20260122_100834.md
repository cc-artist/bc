# 修复寺庙图片匹配错误问题

## 任务描述
统一修复所有寺庙图片，确保每个寺庙的图片都能正确显示，解决图片匹配错误和无法显示的问题。

## 实现步骤

### 1. 统一图片路径
- 将所有寺庙的图片URL替换为已存在的 `/buddha-background.png` 图片
- 确保所有寺庙使用相同的正确图片路径

### 2. 确保图片存在性
- 验证 `/buddha-background.png` 图片存在于public目录中
- 检查图片访问权限和路径格式

### 3. 验证修复效果
- 运行构建命令确保代码无错误
- 确保所有寺庙图片都能正确显示

## 预期效果
- 所有寺庙的图片都能正确显示，不再出现404错误
- 统一的图片显示，避免图片匹配错误
- 稳定的图片加载，提升用户体验

## 涉及文件
- `src/app/components/TempleData.ts`：修改所有寺庙的图片URL

## 技术要点
- 静态资源路径管理
- 统一图片资源使用
- 构建验证和错误检查