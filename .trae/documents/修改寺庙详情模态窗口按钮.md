## 修改计划

### 1. 修改 TempleDetailModal.tsx 文件

**a. 更新接口定义**
- 在 `TempleDetailModalProps` 接口中添加 `onConsultation` 回调函数参数

**b. 修改支付按钮文本**
- 将支付按钮文本从 "支付1万美金" 改为 "支付1万美金购买行程"

**c. 添加咨询客服按钮**
- 在支付按钮旁边添加一个新的 "咨询客服预约行程" 按钮
- 为新按钮添加点击事件处理，调用 `onConsultation` 回调函数

### 2. 修改 TempleSection.tsx 文件

**a. 添加咨询客服处理函数**
- 实现 `handleConsultation` 函数，用于处理咨询客服按钮的点击事件
- 函数内部可以显示一个提示信息，或者调用相应的客服咨询逻辑

**b. 更新组件调用**
- 在 `TempleDetailModal` 组件调用中添加 `onConsultation` 属性，传递 `handleConsultation` 函数

### 3. 调整 CSS 样式（如有需要）

- 确保新添加的咨询客服按钮布局合理，样式与现有按钮协调
- 可以调整按钮容器的样式，确保两个按钮并排显示

### 4. 测试功能

- 确保修改后的支付按钮和咨询客服按钮都能正常工作
- 验证按钮点击事件是否正确触发

## 预期效果

修改后，寺庙详情模态窗口将显示两个按钮：
1. "支付1万美金购买行程" - 用于直接支付购买禅修行程
2. "咨询客服预约行程" - 用于联系客服进行行程预约

这将为用户提供更多选择，既可以直接购买行程，也可以先咨询客服了解更多详情。