我将执行以下修复来解决下载开光结果图片显示比例错误的问题：

1. **修复背景图绘制**：

   * 保持背景图的原始宽高比

   * 居中绘制，避免拉伸

   * 确保覆盖整个Canvas区域或添加适当的背景填充

2. **修复物品图绘制**：

   * 保持物品图的原始宽高比

   * 居中绘制，避免拉伸

   * 确保在佛光效果上方正确显示

3. **优化绘制逻辑**：

   * 计算正确的缩放比例，保持图像原始宽高比

   * 居中定位图像

   * 确保图像不超出Canvas边界

4. **具体修改**：

   * 修改背景图绘制逻辑，添加宽高比保持

   * 修改物品图绘制逻辑，添加宽高比保持

   * 计算正确的绘制尺寸和位置

   * 确保绘制顺序正确

5. **技术实现**：

   * 使用`naturalWidth`和`naturalHeight`获取原始图像尺寸

   * 计算宽高比并确定合适的绘制尺寸

   * 使用`drawImage`的完整参数实现居中绘制

6. **测试和验证**：

   * 确保下载的合成图比例正确，无拉伸

   * 验证不同尺寸和比例的图片都能正确处理

   * 确保背景图和物品图都保持原始

