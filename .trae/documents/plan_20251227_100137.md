1. 修改HeroSection组件，移除直接显示的上传区域
2. 在HeroSection组件中添加onOpenModal函数作为props
3. 在page.tsx中恢复FileUploader组件的调用
4. 添加showUploadModal状态来控制FileUploader的显示
5. 修改FileUploader组件，支持通过props传入初始服务类型
6. 确保点击"赛博佛祖开光"和"求法相"按钮后能正确弹出上传窗口

具体修改点：

* 修改src/app/components/HeroSection.tsx，移除上传区域，添加onOpenModal props

* 修改src/app/page.tsx，恢复FileUploader组件调用，添加showUploadModal状态

* 修改src/app/components/FileUploader.tsx，支持传入initialType参数

预期效果：

* 点击"赛博佛祖开光"按钮后弹出上传窗口，初始服务类型为consecration

* 点击"求法相"按钮后弹出上传窗口，初始服务类型为image

* 上传窗口包含服务类型选择和文件上传功能

* 其他功能保持不变

