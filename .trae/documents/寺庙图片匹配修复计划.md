1. **分析现有图片资源**：项目根目录下已有多个寺庙相关图片，包括南华寺、卧佛寺、国清寺、地藏禅寺、塔尔寺、大昭寺、寒山寺、少林寺等
2. **创建图片目录**：public/temple-images 目录已存在，用于存放寺庙图片
3. **复制图片文件**：将根目录下的寺庙图片复制到 public/temple-images 目录
4. **更新寺庙数据**：修改 TempleData.ts 文件，为每个寺庙分配与其名称匹配的图片URL
5. **验证修复效果**：运行构建命令确保修复成功，所有图片都能正确显示

**具体修改内容**：

* 将每个寺庙的 image 字段从 `/buddha-background.png` 修改为对应的寺庙图片URL，如 `/temple-images/南华寺.webp`

* 确保每个寺庙的图片名称与寺庙名称一致，避免匹配错误

* 对没有对应图片的寺庙，使用合适的默认图片

**预期结果**：

* 所有寺庙在首页和详情窗口中都能显示与其名称匹配的图片

* 图片加载正常，没有404错误

* 构建命令执行成功，没有编译错误

