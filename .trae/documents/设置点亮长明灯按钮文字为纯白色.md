## 现状分析
经过代码检查，发现“点亮长明灯”按钮的文字已经是纯白色，具体情况如下：

1. **HeroSection.tsx 中的按钮**：
   - 文字直接位于按钮内部，没有使用额外的 span 标签
   - 按钮应用了 `cta-button` 类，该类设置了 `color: #ffffff`
   - 按钮还应用了 `silver-white-text` 类，该类也设置了 `color: #ffffff !important`
   - 背景渐变动画不影响文字颜色

2. **page.tsx 中的按钮**：
   - 中文文字使用 `<span className={styles.chineseText}>`，该类设置了 `color: #FFFFFF`
   - 英文文字使用 `<span className={styles.englishText}>`，该类也设置了 `color: #FFFFFF`
   - 按钮位于 `lightLampContainer` 容器内，没有其他样式覆盖文字颜色

## 结论
“点亮长明灯”按钮的中、英文字已经是纯白色，无需进行额外修改。如果您希望确认效果，可以通过前端预览查看实际渲染结果。

## 建议
由于文字已经是纯白色，建议保持当前状态不变，避免不必要的代码修改。