# 实现方案

## 核心需求
1. 保持代表神秘能量的抽象佛祖星空图
2. 将祝福语和当天日期显示为繁体宋体字
3. 移除祝福语和日期的背景或底纹边框

## 实现步骤

### 1. 修改CSS样式
- **文件**: `src/app/page.module.css`
- **修改内容**: 
  - 移除`.buddhistQuote`的背景、边框和内边距
  - 移除`.todayDate`的背景、边框和内边距
  - 保持繁体宋体字设置
  - 增强文字阴影效果，确保在深色背景上清晰可见
  - 调整文字位置，确保美观

### 2. 确保图片显示正常
- **修改内容**: 
  - 保持当前的抽象佛祖星空图显示
  - 确保图片容器样式不变
  - 确保图片覆盖层样式适合新的文字显示

## 代码修改要点

1. **CSS样式调整**: 移除背景和边框，增强文字阴影
2. **文字样式**: 保持繁体宋体字
3. **布局调整**: 确保文字在图片上的位置美观
4. **响应式设计**: 确保在不同设备上都有良好的显示效果

## 预期效果
当用户打开"南无阿弥陀佛"窗口时，会看到：
- 一张代表神秘能量的抽象佛祖星空图
- 祝福语显示为繁体宋体字，位于图片上部居中位置，无背景和边框
- 当天日期显示为繁体宋体字，位于图片底部，无背景和边框
- 文字清晰可见，与图片风格协调
- 文字带有适当的阴影，增强可读性

## 技术细节
- 使用CSS移除背景和边框
- 增强文字阴影效果，确保在深色背景上清晰可见
- 保持当前的图片显示逻辑
- 保持当前的日期格式化逻辑