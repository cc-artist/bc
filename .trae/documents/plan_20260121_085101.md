## 问题分析
当前代码中包含两个音频播放方案：
1. **B站视频音频**：通过iframe加载BV1rBCsB1E42视频
2. **备用音频**：generateMoneyBackMyHomeSound函数使用Web Audio API生成音乐

用户要求只保留B站视频音频，删除其他音频。

## 修改计划

### 1. 删除generateMoneyBackMyHomeSound函数
- 删除第364-583行的generateMoneyBackMyHomeSound函数定义

### 2. 删除对备用音频函数的调用
- 删除第1503行的`generateMoneyBackMyHomeSound()`调用
- 删除第1507行的`generateMoneyBackMyHomeSound()`调用
- 删除第1598行的`generateMoneyBackMyHomeSound()`调用
- 删除第1602行的`generateMoneyBackMyHomeSound()`调用

### 3. 清理playAudio函数
- 简化playAudio函数，只保留B站播放器播放逻辑
- 移除所有对备用音频的引用

### 4. 保留的内容
- B站播放器iframe
- 音频控制按钮
- 播放命令发送逻辑

## 验证方法
修改后：
- 页面加载时不播放音频
- 点击"点亮长明灯"按钮后才播放B站视频音频
- 页面中没有任何其他音频生成代码